// Shared Tailwind class constants for consistent styling across the application

// Section title - used for all major section headings  
export const sectionTitleClasses = "text-3xl md:text-4xl font-medium text-charcoal font-['Richmond'] not-italic";

// Banner title - used for hero/banner sections with white text (same size as section titles)
export const bannerTitleClasses = "text-3xl md:text-4xl font-medium !text-white font-['Richmond'] not-italic";

// Section title for white text (keeping for backwards compatibility)
export const sectionTitleWhiteClasses = "text-3xl md:text-4xl font-medium text-white font-['Richmond'] not-italic";

// Subsection title - used for smaller section headings
export const subsectionTitleClasses = "text-2xl md:text-3xl font-semibold text-charcoal";

// Body text
export const bodyTextClasses = "text-base text-body leading-relaxed";